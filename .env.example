# CLI: LLM_API_KEY обязателен. Веб-API (SaaS): BYOK в профиле пользователя.

LLM_API_KEY=sk-or-...
LLM_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=openai/gpt-4.1-mini
# Опционально: более точный веб-поиск (https://tavily.com)
# Без ключа используется DuckDuckGo
TAVILY_API_KEY=
# Авиа API (цены, пересадки): https://www.travelpayouts.com/developers/api
# Без ключа — только deep links на агрегаторы
TRAVELPAYOUTS_API_KEY=
# Affiliate: marker обязателен; trs — для Partner Links API (Tutu, Booking)
TRAVELPAYOUTS_MARKER=
TRAVELPAYOUTS_TRS=
AFFILIATE_ENABLED=true
AFFILIATE_AVIASALES=true
AFFILIATE_TUTU_BUS=true
# Жд Tutu — только ИП в реестре Tutu
AFFILIATE_TUTU_TRAIN=false
# Отели: affiliate deep links на Booking.com (вкладка «Отели»)
AFFILIATE_BOOKING=false
# Admin token для GET /api/affiliate/metrics и POST /api/affiliate/sync
AFFILIATE_ADMIN_TOKEN=
# Курс USD→RUB для revenue_rub в sync (если TP отдаёт USD)
AFFILIATE_USD_RUB_RATE=90

# SQLite: поездки, предпочтения, версии программы
DATABASE_PATH=data/trips.db

# SaaS API (обязательно для uvicorn api.main:app)
JWT_SECRET=change-me-to-random-64-chars
JWT_ACCESS_TTL_MINUTES=60
# Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SETTINGS_ENCRYPTION_KEY=
# Google OAuth (опционально)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback
# Прод-домен фронтенда через запятую
CORS_ORIGINS=

# LangSmith (опционально): трейсы графа
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=tourist-assistant

# LangFuse (self-hosted, опционально): трейсы через LangChain callbacks
LANGFUSE_ENABLED=false
# Локальный запуск: python3 main.py
LANGFUSE_HOST=http://localhost:3000
# Docker: docker compose run --rm app (compose подставит этот URL)
LANGFUSE_HOST_DOCKER=http://host.docker.internal:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
